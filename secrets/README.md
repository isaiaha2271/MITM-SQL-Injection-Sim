# Local Docker Secrets

This folder stores local secret files used by Docker Compose.

Real secret files are intentionally not committed to Git. Create them locally before running the lab.

## Required files

Create the following files:

```text
secrets/mysql_password.txt
secrets/mysql_root_password.txt